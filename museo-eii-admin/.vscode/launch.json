{
    "configurations": [
        {
            "name": "Launch Edge",
            "request": "launch",
            "type": "pwa-msedge",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}"
        },
    ]
}